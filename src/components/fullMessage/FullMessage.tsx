import styles from "./fullMessage.module.css"

const  FullMessage = () => {
    return (
        <div className={styles.container}>
            FullMessage   
        </div>
    )
};

export default FullMessage;